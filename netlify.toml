# netlify.toml (Simplified Version)

[build]
  functions = "netlify/functions/"
  publish = "."

# This rule makes sure all paths work correctly on a single domain
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
