# netlify.toml
[build]
  functions = "netlify/functions/"
  publish = "." # This tells Netlify to publish the contents of the root folder
# redirect rule 

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
